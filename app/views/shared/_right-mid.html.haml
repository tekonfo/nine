.message{"data-message-id": "#{message.id}"}
  %p.name-write--name
    = message.user.name
  %p.name-write--date
    = message.updated_at.strftime('%Y/%m/%d %H:%M')
  %p.name-write--small
    - if message.text?
      = message.text
    - if message.image?
      = image_tag message.image.to_s
